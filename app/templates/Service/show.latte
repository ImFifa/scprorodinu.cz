{block content}

<section class="service-page" id="service-page">
    <div class="container">
        <h1 n:block="name">{$service->name}</h1>
        <hr>
        <div class="content">
            {$service->content|noescape}
        </div>

        <div n:if="$service->gallery_id != NULL" class="galerie">
            <h3 id="galerie">Galerie</h3>

            <div class="row no-gutters">
                {foreach $images as $image}
                    {if $iterator->counter < 5}
                        <a href="{$basePath}/upload/services/{$service->gallery_id}/{$image->name}" class="col-xsm-6 gal-img col-lg-3" data-lightbox="gallery" data-name="{$image->name}">
                            <img data-src="{$basePath}/upload/services/{$service->gallery_id}/thumbs/{$image->name}" class="lazyload img-fluid" alt="{$image->name}">
                            <div n:if="$iterator->counter === 4" class="n-of-photos">{count($images) - 3}
                                <small>{if count($images) <= 7}další fotografie{else}dalších fotografií{/if}</small>
                            </div>
                        </a>
                    {else}
                        <a href="{$basePath}/upload/galleries/{$service->gallery_id}/{$image->name}" data-lightbox="gallery" data-name="{$image->name}"></a>
                    {/if}
                {/foreach}
            </div>
        </div>


        <a n:href="Service:" class="btn btn-primary">Zpět na aktivity/služby</a>
    </div>
</section>